## Aggregation Pipeline Builder

Create an API endpoint that uses MongoDB aggregation to generate analytics: group orders by user, calculate total sales per category, find top 5 customers by spending. Include $lookup to join collections and $group for statistics.