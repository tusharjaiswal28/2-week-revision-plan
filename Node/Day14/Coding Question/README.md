## Real-time Progress Tracker

Create a file upload endpoint that uses Socket.io to emit progress updates to the client. Track upload percentage and completion status. Include error handling and support for multiple simultaneous uploads.