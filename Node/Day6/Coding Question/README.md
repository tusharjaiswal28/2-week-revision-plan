## Event-Driven File Watcher

Create a file watcher using fs.watch() and EventEmitter. Emit custom events when files are added, modified, or deleted in a directory. Create listeners that log these events with timestamps. Include error handling.