## File Processing with Streams

Create an API endpoint that reads a large CSV file using streams, processes each line (e.g., converts to uppercase), and writes to a new file. Track progress and return percentage completion. Handle errors properly.