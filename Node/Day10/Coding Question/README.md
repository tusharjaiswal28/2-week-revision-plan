## MongoDB CRUD Operations

Using MongoDB (or in-memory mock), create a simple API for a product catalog. Implement endpoints for create, read (with filters), update, and delete. Include pagination, sorting, and basic text search functionality.