## Validation & Error Handler System

Create a validation middleware using a validation library (or custom logic) for user registration (email, password strength, age). Implement a centralized error handler that formats errors consistently. Include custom error classes for different error types.